# Pipeline dependencies (pipeline/ package).
#
# The geometry layer — camera model, Tier 1 meshing, RANSAC plane fitting,
# the synthetic test room — needs only the first four and runs fine on a
# laptop with no GPU. That is deliberate: it means the scripts/selftest_*.py
# ground-truth checks can be run anywhere, without Colab.
numpy
pillow
trimesh
scipy

# Edge-preserving depth filtering, mask cleanup, occlusion measurement.
# Optional — every call site degrades gracefully without it — but the
# results are noticeably better with it.
opencv-python-headless

# Only needed to run Depth Anything V2 locally instead of on Colab, e.g.
#   python scripts/seed_demo.py --image photo.jpg
# torchvision is NOT optional despite never being imported directly —
# transformers' AutoImageProcessor requires it and fails at load time
# without it. Verified working CPU-only on a GTX 1650 machine with the
# metric-indoor-small checkpoint (~15s per 960x1280 photo).
#   pip install transformers
#   pip install torchvision --index-url https://download.pytorch.org/whl/cpu
#
# SAM2 and TripoSR are Colab-only — do not expect them to fit in 4GB VRAM.
